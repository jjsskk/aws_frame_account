import 'package:flutter/material.dart';

class UserCareInfoPage extends StatelessWidget {
  const UserCareInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('이용자 돌봄 정보'),
        centerTitle: true,
      ),
      body: Center(
        child: Column(
          children: [

          ],
        ),
      ),
    );;
  }
}
