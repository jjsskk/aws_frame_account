import 'package:flutter/material.dart';

class BrainSignalPage extends StatelessWidget {
  const BrainSignalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('뇌 신호 그래프'),
        centerTitle: true,
      ),
      body: Column(
        children: [

          ],
      ),
    );
  }
}
